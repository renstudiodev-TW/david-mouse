# David Mouse 進度日誌

新的在上面，舊的在下面。開工時讀最上面那一筆。

> 這個專案的 `docs/` 是對外的教學網站（Netlify 部署，已在 `.gitignore` 排除），
> 所以團隊文件不放 `docs/team/`，避免內部決策被公開到 davidmouse.renstudio.tw。
> 決策簿放在專案根目錄的 `DECISIONS.md`。

---

## 2026-09-01

### 做了什麼

- **新增語音聽寫按鈕**。全模式與精簡模式都加了「🎙 語音聽寫」，觸發方式跟左鍵／右鍵一樣有倒數，時間到送出 `Ctrl+/` 組合鍵，讓使用者可以用頭控滑鼠觸發 ChatGPT 桌面版的語音聽寫快捷鍵打字。`src/win32_input.py` 新增 `dictation()`：**先在游標當時位置點一下左鍵取得焦點，再送出 Ctrl+/**——鍵盤事件只會送到目前有焦點的視窗、不會跟著游標跑，一開始漏了這一步，RC 直接問「會不會觸發左鍵」才抓到，細節見 D-007。
- **精簡模式也保留這顆按鈕**（RC 建議）。實測過 Tk 排版：精簡視窗高度 180→300，暫停鍵 88px、聽寫鍵 70px，都超過無障礙設計要求的 60px 門檻。全模式視窗 540→600。四語系字串補齊。
- **打包「支援 ChatGPT 語音輸入版」，不動原本的 `david-mouse.zip`**。新增 `build-chatgpt.bat` 打包出 `david-mouse-chatgpt.zip`，以**額外附件**方式上傳到既有的 v1.1.0 GitHub Release（同一個 tag，不建新 release），原本的 `david-mouse.zip` 這次全程 byte-for-byte 沒被動過。決策見 D-008。
- **網站四語系全面更新**：下載區加「支援 ChatGPT 語音輸入版」卡片；核心功能截圖全部重拍（含新按鈕）；新增「三種顯示模式」截圖區（完整／精簡／觀影）；精簡模式與觀影模式補上教學說明卡片與步驟。四語系（zh-TW/en/ja/ko）同步更新，簡體字掃描過（唯一命中的是既有日文頁面本來就有的合法日文漢字，不是簡體字誤用）。
- **截圖改用 `PrintWindow` 直接向視窗要圖**，不再用桌面截圖。這台環境的桌面截圖工具拍不到 Tkinter 視窗本尊（會拍到 Claude Code 自己的畫面），改用 Win32 API `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` 繞過桌面畫面合成，才拿得到真實渲染內容。之後這個專案要生成介面截圖都可以沿用這個做法。
- **修掉系統管理員身分開不起來的 bug**（RC 回報：明星三缺一過去要用系統管理員身分開才點得到，這次卻打不開）。根源是先前合併但沒正式發版的單一實例鎖（4e0fbe2），只要偵測到已有一份在跑就無條件擋掉新的一份，沒有分辨新開的這份是不是系統管理員——這正是上一則收工筆記裡「沒實測到的組合」。`src/single_instance.py` 新增 `_take_over_from_lower_privilege()`：搶鎖失敗時如果目前 process 是系統管理員，就直接關掉舊程序等它結束再接手；方向反過來則維持原樣繼續擋。決策見 D-009。加了會真的另開子程序模擬情境的迴歸測試，15 個測試全過。

### 決策

- Ctrl+/ 這類鍵盤快捷鍵，觸發前一定要先在目標位置點一下取得焦點，不能只送鍵盤事件（D-007）。
- 平台變體（ChatGPT 版）用「同一個 release 多一個 asset」而不是新開 tag/release，確保不動到已發布的原始資產（D-008）。
- 單一實例鎖的接管方向不對稱：只有系統管理員能接管一般權限的舊實例，反過來不行（D-009）。

### 未完成

- **RC 還沒實際重新測過**：修好的 ChatGPT 版 zip 已經重新上傳，但明星三缺一那個情境是 RC 回報的，還沒等到他重新下載測試確認真的解決。
- **v1.1.1（單一實例修正本體）仍未正式發版**，這次連同接管修正一起打包進了「ChatGPT 語音輸入版」zip，但**原本的 `david-mouse.zip` 還是 v1.1.0 舊版，兩者程式碼已經不同步**。等 RC 確認沒問題後，要跟他確認要不要把這些修正也發成正式的 v1.1.1／v1.2.0，讓兩個下載版本重新對齊。
- 接管邏輯只驗證了「模擬情境」（monkeypatch `is_admin()` + 真的另開子程序），沒有實際觸發過 UAC 提示做端到端測試，因為這個環境沒辦法互動式點 UAC 對話框。邏輯本身經過驗證，但如果 David 的電腦上還有沒設想到的邊界情況（例如兩個都是管理員身分互搶），值得再留意一次。
- 產品化檢查表沒有逐項對過，尤其這次新增的截圖產生流程（`PrintWindow`）是這個環境特有的變通做法，換一台機器不一定能重現，沒有寫進任何自動化腳本，純粹是這次手動跑的步驟。

### 卡點

無（今天的卡點都在對話過程中排除了：截圖抓不到真實視窗、Ctrl+/ 送錯焦點、單一實例擋住管理員重開，三個都找到根因並修掉）。

### 下一步

1. 等 RC 在會用到明星三缺一（或其他需要管理員權限點擊的情境）的電腦上重新測過 ChatGPT 語音輸入版，確認系統管理員接管邏輯真的解決問題。
2. 確認沒問題後，跟 RC 討論要不要把這幾個修正（語音聽寫、單一實例接管修正）也發成正式版本更新原本的 `david-mouse.zip`，避免兩個下載版本長期不同步。
3. RC 之後想聊「發表在國外哪些網站增加觸及」，等 bug 確認修好後再回頭處理，這次收工前沒有做。

---

## 2026-08-21

### 做了什麼

- **開機以系統管理員權限自動啟動**。`src/autostart.py` 改成雙模式：程式本身以系統管理員身分執行時，用 `schtasks /Create /XML` 註冊名為 `DavidMouse` 的工作排程（登入時觸發、`HighestAvailable`、延遲 15 秒）；否則退回原本的 Startup 資料夾 `.lnk` 捷徑。兩者互斥，建立其中一個時會清掉另一個。
- **一鍵設定腳本**。`setup-admin-autostart.bat` 與 `remove-admin-autostart.bat`（純 ASCII，會自行提權）加上實作本體 `tools/admin-autostart.ps1`，讓不想重新打包 exe 的情況也能設定。`build.bat` 已把三支腳本一起包進 release zip。
- **UI 與 i18n**。按鈕新增「開（管理員）」狀態，四語系字串到齊。App 啟動時會以系統現況校正設定檔，避免使用者手動刪掉排程後狀態不同步。
- **發布 v1.1.0**。合併進 main、重新打包 exe、發 GitHub Release，網站四個語系頁面補上管理員模式說明並部署到 Netlify。
- **修掉多重實例 bug**。新增 `src/single_instance.py`，用 `%APPDATA%\DavidMouse\instance.lock` 的獨占鎖擋掉第二份；搶不到鎖就把先開的視窗叫到前面並閃標題列，自己安靜退場。補 5 個測試，測試總數 9 到 14。

### 決策

- 開機提權自動啟動改用工作排程器而非啟動資料夾捷徑（詳見 `DECISIONS.md` D-004）。
- 單一實例用鎖檔而非具名 Mutex（D-005）。
- 團隊文件放專案根目錄而非 `docs/team/`（D-006）。

### 未完成

- **v1.1.1 尚未發布**。單一實例修正已合併 main（commit `4e0fbe2`）、exe 已重新打包在 `dist\david-mouse.exe`，但依 RC 指示暫不發 Release，等他自己在 David 的電腦上測過再決定。網站下載鍵目前仍指向 v1.1.0。
- **一種組合沒實測到**：排程工作開的「管理員實例」加上使用者手動開的「一般實例」。要提權才測得到，需要有人按 UAC。理論上鎖檔在使用者自己的 AppData 底下、兩種權限都存取得到所以會擋下來，但值得在 David 的電腦上實際試一次。就算真的沒擋到，結果也只是回到修正前的行為，不會更糟。
- 產品化檢查表沒有逐項對過。這是一個已經交付給實際使用者的工具，下次值得花一輪把邊界情境與已知限制寫清楚。

### 卡點

無。

### 下一步

1. RC 在 David 的電腦上驗證單一實例（開起來後再點一次捷徑，應該只會閃一下不會多開）。
2. 驗過就發 v1.1.1 Release，網站下載鍵會自動指向新版（不需要重新部署 Netlify，除非要在網站上補說明）。
3. 提醒使用者換版時要把舊的完全關掉，軟導航式的重開不保證拿到新的執行檔。

### 環境備註

- 打包用的是 scoop 那支 Python（`C:\Users\boren\scoop\apps\python\current`，`pyinstaller` 裝在那裡）。今天補裝了 `pynput` 進去，之前缺這個所以打包會失敗。
- 專案放在 `C:\Users\boren\Desktop\fordavicdmouse`，不在全域預設的 `C:\RenStudio\case` 底下。這是既有狀態，沒有搬動。
