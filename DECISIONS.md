# David Mouse 決策簿

記錄技術選型、規格取捨與資料模型變動，以及**為什麼**這樣選。開工時先讀這份，避免重新討論已經定案的事。

D-001 到 D-003 是回頭整理自 `DESIGN.md` 的既有決策，日期是專案初期，不是當天現場記錄。D-004 之後為即時記錄。

---

## D-001　UI 用 Tkinter，不用 Electron 或 PySide6

**日期**：2026-05-28（整理自 `DESIGN.md`）

Tkinter 是 Python 標準函式庫，零額外依賴，打包出來的 exe 才壓得住體積。目標使用者是頭控滑鼠的身障者，工具必須「下載就能用、不用安裝」。Electron 動輒上百 MB，PySide6 授權與體積都是負擔。

**限制**：Tkinter 的外觀不精緻。這個專案用大按鈕、高對比，本來就不追求視覺花俏，所以可以接受。

## D-002　點擊模擬走 ctypes 直接呼叫 Win32 `SendInput`

**日期**：2026-05-28（整理自 `DESIGN.md`）

輔具場景不能容忍漏點。`SendInput` 是最底層也最穩定的做法。全域滑鼠 hook 則用 `pynput` 包裝的 `SetWindowsHookEx`，不自己手寫 ctypes callback，避免 callback 被垃圾回收造成當機。

## D-003　開機自動啟動不寫登錄檔

**日期**：2026-05-28（整理自 `DESIGN.md`）

使用者的電腦常有家屬或照服員在管理，動登錄檔的東西一旦出問題很難解釋也很難還原。改用檔案系統層級的做法（啟動資料夾捷徑），使用者自己看得到、刪得掉。

---

## D-004　開機以系統管理員權限啟動，改用工作排程器

**日期**：2026-08-21

**背景**：RC 回報有些視窗要 David Mouse 自己也用系統管理員權限執行才點得動（模擬點擊被 UIPI 擋掉）。手動用「以系統管理員身分執行」開可以解決，但需要每次都這樣開。

**決策**：以系統管理員權限執行時，註冊名為 `DavidMouse` 的工作排程（`LogonTrigger` + `HighestAvailable`）；沒有管理員權限時退回原本的啟動資料夾捷徑。兩種模式互斥。

**為什麼不繼續用啟動資料夾**：Windows 的啟動資料夾**沒辦法**啟動需要提權的程式。捷徑就算勾了「以系統管理員身分執行」再放進去，UAC 會直接擋掉，程式根本不會跑起來，而且不會有任何錯誤訊息。這條路是死的，不要再試。工作排程器是唯一能在登入時取得管理員權限又不跳 UAC 的方式。

**為什麼不在 exe 加 UAC manifest**：那會讓每次手動開啟都跳 UAC 對話框。UAC 跑在安全桌面上，對頭控滑鼠使用者是很高的操作門檻。維持 exe 不提權、用排程工作提供靜默提權，體驗最好。

**排程設定刻意覆寫的三個預設值**（`schtasks` 的預設會害人）：

| 設定 | 預設 | 改成 | 為什麼 |
|---|---|---|---|
| `DisallowStartIfOnBatteries` | true | false | 筆電沒插電就不啟動 |
| `StopIfGoingOnBatteries` | true | false | 拔掉插頭程式會被停掉 |
| `ExecutionTimeLimit` | P3D | PT0S | 長時間開機的電腦跑滿三天會被強制結束 |

另外加了 `PT15S` 的登入延遲，讓桌面先載入完成，避免視窗被其他開機程式蓋掉。

**授權限制**：建立最高權限的排程工作本身需要管理員權限。所以 App 內的切換鈕只有在程式是用管理員身分開的時候才建得起來，否則會安靜退回一般權限模式。另外提供 `setup-admin-autostart.bat`（自行提權）給不想重新打包 exe 的情況。

## D-005　單一實例用鎖檔，不用具名 Mutex

**日期**：2026-08-21

**背景**：RC 回報程式沒有限制只能開一份。使用者在程式已經縮在角落時再點一次捷徑，就會跑出兩份，一份在自動點擊、另一份顯示暫停中，畫面互相矛盾。功能雖然不受影響，但對頭控使用者非常混亂。

**決策**：在 `%APPDATA%\DavidMouse\instance.lock` 上用 `msvcrt.locking` 取得獨占鎖。搶不到就代表已經有一份在跑，讀 `instance.pid` 找出對方視窗，`ShowWindow` + `SetForegroundWindow` + `FlashWindowEx` 叫到前面，自己退場。

**為什麼不用具名 Mutex**（這是這個決策的重點）：排程工作啟動的那份是**高完整性等級**，使用者手動開的是**中等級**。低等級的程序不一定打得開高等級程序建立的核心物件，`CreateMutexW` 會拿不到 `ERROR_ALREADY_EXISTS`，誤判成「沒有其他實例」，於是照樣開出第二份。鎖檔放在使用者自己的 AppData 底下，兩種權限都存取得到，沒有這個問題。

**為什麼不跳訊息框**：使用者還要想辦法把對話框關掉，對頭控操作是額外負擔。安靜退場並把原視窗叫到前面加閃爍，比較符合直覺。

**當機不會留下死鎖**：檔案鎖由作業系統在程序結束時釋放，強制砍掉程序後重開一樣正常。實測驗證過。

## D-006　團隊文件放專案根目錄，不放 `docs/team/`

**日期**：2026-08-21

這個專案的 `docs/` 是對外的教學網站原始碼，用 `netlify deploy --dir=.` 從本機直接部署，而且已經在 `.gitignore` 排除（不進 GitHub）。如果照全域慣例把團隊文件放 `docs/team/`，會有兩個問題：一是被 gitignore 吃掉不進版控，二是 Netlify 部署時整包上傳，內部決策會被公開到 davidmouse.renstudio.tw。

所以 `WORKLOG.md` 與 `DECISIONS.md` 放專案根目錄，跟既有的 `DESIGN.md`、`DEV_LOG.md` 同一層，符合這個 repo 原本的扁平文件慣例。
