# David Mouse 決策簿

記錄技術選型、規格取捨與資料模型變動，以及**為什麼**這樣選。開工時先讀這份，避免重新討論已經定案的事。

D-001 到 D-003 是回頭整理自 `DESIGN.md` 的既有決策，日期是專案初期，不是當天現場記錄。D-004 之後為即時記錄。

---

## D-001　UI 用 Tkinter，不用 Electron 或 PySide6

**日期**：2026-05-28（整理自 `DESIGN.md`）

Tkinter 是 Python 標準函式庫，零額外依賴，打包出來的 exe 才壓得住體積。目標使用者是頭控滑鼠的身障者，工具必須「下載就能用、不用安裝」。Electron 動輒上百 MB，PySide6 授權與體積都是負擔。

**限制**：Tkinter 的外觀不精緻。這個專案用大按鈕、高對比，本來就不追求視覺花俏，所以可以接受。

## D-002　點擊模擬走 ctypes 直接呼叫 Win32 `SendInput`

**日期**：2026-05-28（整理自 `DESIGN.md`）

輔具場景不能容忍漏點。`SendInput` 是最底層也最穩定的做法。全域滑鼠 hook 則用 `pynput` 包裝的 `SetWindowsHookEx`，不自己手寫 ctypes callback，避免 callback 被垃圾回收造成當機。

## D-003　開機自動啟動不寫登錄檔

**日期**：2026-05-28（整理自 `DESIGN.md`）

使用者的電腦常有家屬或照服員在管理，動登錄檔的東西一旦出問題很難解釋也很難還原。改用檔案系統層級的做法（啟動資料夾捷徑），使用者自己看得到、刪得掉。

---

## D-004　開機以系統管理員權限啟動，改用工作排程器

**日期**：2026-08-21

**背景**：RC 回報有些視窗要 David Mouse 自己也用系統管理員權限執行才點得動（模擬點擊被 UIPI 擋掉）。手動用「以系統管理員身分執行」開可以解決，但需要每次都這樣開。

**決策**：以系統管理員權限執行時，註冊名為 `DavidMouse` 的工作排程（`LogonTrigger` + `HighestAvailable`）；沒有管理員權限時退回原本的啟動資料夾捷徑。兩種模式互斥。

**為什麼不繼續用啟動資料夾**：Windows 的啟動資料夾**沒辦法**啟動需要提權的程式。捷徑就算勾了「以系統管理員身分執行」再放進去，UAC 會直接擋掉，程式根本不會跑起來，而且不會有任何錯誤訊息。這條路是死的，不要再試。工作排程器是唯一能在登入時取得管理員權限又不跳 UAC 的方式。

**為什麼不在 exe 加 UAC manifest**：那會讓每次手動開啟都跳 UAC 對話框。UAC 跑在安全桌面上，對頭控滑鼠使用者是很高的操作門檻。維持 exe 不提權、用排程工作提供靜默提權，體驗最好。

**排程設定刻意覆寫的三個預設值**（`schtasks` 的預設會害人）：

| 設定 | 預設 | 改成 | 為什麼 |
|---|---|---|---|
| `DisallowStartIfOnBatteries` | true | false | 筆電沒插電就不啟動 |
| `StopIfGoingOnBatteries` | true | false | 拔掉插頭程式會被停掉 |
| `ExecutionTimeLimit` | P3D | PT0S | 長時間開機的電腦跑滿三天會被強制結束 |

另外加了 `PT15S` 的登入延遲，讓桌面先載入完成，避免視窗被其他開機程式蓋掉。

**授權限制**：建立最高權限的排程工作本身需要管理員權限。所以 App 內的切換鈕只有在程式是用管理員身分開的時候才建得起來，否則會安靜退回一般權限模式。另外提供 `setup-admin-autostart.bat`（自行提權）給不想重新打包 exe 的情況。

## D-005　單一實例用鎖檔，不用具名 Mutex

**日期**：2026-08-21

**背景**：RC 回報程式沒有限制只能開一份。使用者在程式已經縮在角落時再點一次捷徑，就會跑出兩份，一份在自動點擊、另一份顯示暫停中，畫面互相矛盾。功能雖然不受影響，但對頭控使用者非常混亂。

**決策**：在 `%APPDATA%\DavidMouse\instance.lock` 上用 `msvcrt.locking` 取得獨占鎖。搶不到就代表已經有一份在跑，讀 `instance.pid` 找出對方視窗，`ShowWindow` + `SetForegroundWindow` + `FlashWindowEx` 叫到前面，自己退場。

**為什麼不用具名 Mutex**（這是這個決策的重點）：排程工作啟動的那份是**高完整性等級**，使用者手動開的是**中等級**。低等級的程序不一定打得開高等級程序建立的核心物件，`CreateMutexW` 會拿不到 `ERROR_ALREADY_EXISTS`，誤判成「沒有其他實例」，於是照樣開出第二份。鎖檔放在使用者自己的 AppData 底下，兩種權限都存取得到，沒有這個問題。

**為什麼不跳訊息框**：使用者還要想辦法把對話框關掉，對頭控操作是額外負擔。安靜退場並把原視窗叫到前面加閃爍，比較符合直覺。

**當機不會留下死鎖**：檔案鎖由作業系統在程序結束時釋放，強制砍掉程序後重開一樣正常。實測驗證過。

## D-006　團隊文件放專案根目錄，不放 `docs/team/`

**日期**：2026-08-21

這個專案的 `docs/` 是對外的教學網站原始碼，用 `netlify deploy --dir=.` 從本機直接部署，而且已經在 `.gitignore` 排除（不進 GitHub）。如果照全域慣例把團隊文件放 `docs/team/`，會有兩個問題：一是被 gitignore 吃掉不進版控，二是 Netlify 部署時整包上傳，內部決策會被公開到 davidmouse.renstudio.tw。

所以 `WORKLOG.md` 與 `DECISIONS.md` 放專案根目錄，跟既有的 `DESIGN.md`、`DEV_LOG.md` 同一層，符合這個 repo 原本的扁平文件慣例。

## D-007　鍵盤快捷鍵要先點擊取得焦點，不能只送按鍵事件

**日期**：2026-09-01

**背景**：新增「語音聽寫」按鈕送出 `Ctrl+/` 觸發 ChatGPT 桌面版聽寫。第一版只單純送出這個組合鍵，RC 直接問「這個按鈕會不會觸發左鍵點一下」，順著這個問題才發現問題：鍵盤事件（`SendInput` 的 `KEYBDINPUT`）只會送到**目前有焦點的視窗**，不會跟著滑鼠游標跑。左鍵／右鍵／雙擊按鈕的「倒數後移動游標、時間到才觸發」模式對滑鼠點擊沒問題，因為點擊本身就是在游標當時位置發生、順便完成了焦點轉移；但純鍵盤事件沒有這個副作用，按下 David Mouse 按鈕當下焦點在 David Mouse 自己身上，使用者倒數期間把游標移到 ChatGPT 上並不會把焦點也帶過去（Windows 預設「點了才有焦點」，不是「滑鼠移過去就有焦點」）。

**決策**：`win32_input.dictation()` 在送出 `Ctrl+/` 之前，先在游標當時位置補一個 `left_click()`，讓目標視窗（例如 ChatGPT 輸入框）先拿到焦點，再送快捷鍵。

**如何應用**：以後任何要新增「送鍵盤快捷鍵到某個外部視窗」的功能，都要走這個「先點擊取得焦點、再送按鍵」的模式，不能只複製滑鼠點擊按鈕的倒數框架就直接送鍵盤事件。

## D-008　平台變體用同一個 Release 多加一個 asset，不開新 tag

**日期**：2026-09-01

**背景**：RC 要求新增「支援 ChatGPT 語音輸入版」，但明確交代「不要覆蓋原本的版本，直接新增一個新的連結」。網站主要下載鍵用 `releases/latest/download/david-mouse.zip`，如果開一個新的 GitHub Release（新 tag），它會自動變成新的 `latest`，除非那個新 release 裡也放一份一模一樣的 `david-mouse.zip`，否則主下載鍵取得的內容會被換掉，等於間接「覆蓋」了原本的版本。

**決策**：不開新 tag、不建新 release，直接 `gh release upload v1.1.0 dist/david-mouse-chatgpt.zip`，把新變體以**不同檔名的額外 asset** 掛進現有的 v1.1.0 release。`david-mouse.zip` 這個檔名、這個 release 完全沒被動到，`releases/latest/download/david-mouse.zip` 的行為 100% 不變；網站新增的第二顆下載鍵改指向 `releases/latest/download/david-mouse-chatgpt.zip`。

**取捨**：這個做法簡單、零風險，但代價是兩個 zip 現在的原始碼版本不同步（`david-mouse.zip` 還停在 v1.1.0 當時的 commit，`david-mouse-chatgpt.zip` 是今天 main 最新的 commit，包含單一實例接管修正等後續變更）。等 RC 確認這些新變更沒問題後，需要另外決定要不要把兩者重新對齊、發一個真正的新版本號。

## D-009　單一實例接管邏輯：只有系統管理員能接管，方向不對稱

**日期**：2026-09-01

**背景**：D-005 的鎖檔機制上線後（4e0fbe2，尚未正式發版），RC 回報某些遊戲（明星三缺一）需要 David Mouse 以系統管理員身分執行才點得到，但改用系統管理員身分重開時，會被自己已經開著的一般權限舊實例擋下來，只是把舊視窗叫到前面、新的系統管理員實例悄悄退出。這正是 D-005 寫下時就標記「沒實測到」的那個組合：管理員實例與一般實例並存的情境。

**決策**：`single_instance.acquire()` 搶鎖失敗時，如果目前這個 process 本身是系統管理員（`autostart.is_admin()`），就呼叫 `_terminate_pid()` 強制關掉舊的那份、等它真的結束（`WaitForSingleObject`）再重新搶鎖接手。方向刻意不對稱：**一般權限的新實例遇到系統管理員的舊實例時，維持原樣繼續擋**，不嘗試接管。

**為什麼方向不對稱**：這不只是設計選擇，也是 Windows 權限模型本身的限制。較低完整性等級的程序原本就打不開較高等級程序的控制代碼（`OpenProcess` 會失敗），就算想讓一般權限接管系統管理員的舊實例也做不到；而系統管理員身分通常可以順利終止同使用者底下的一般權限程序。讓程式碼的行為跟著 OS 權限模型走，而不是逆著它假裝能做到做不到的事。

**安全考量**：`_take_over_from_lower_privilege()` 讀到的 pid 若剛好等於自己的 pid（理論上不該發生，但寧可多防一層），會直接放棄接管，避免程序把自己終止掉。

**驗證方式**：因為這個環境沒辦法互動觸發 UAC 提示，沒辦法端到端測試「真的按右鍵→以系統管理員身分執行」。改用 monkeypatch `autostart.is_admin` 回傳 `True`，搭配真的另外開一個子程序模擬「已在跑的一般權限實例」，驗證接管邏輯本身正確（子程序真的被關掉、鎖真的轉移成功）。邏輯層面驗證過，但仍建議 RC 在會用到系統管理員權限情境的電腦上實際跑一次確認。
